# ─────────────────────────────────────────
#  STUDEX BACKEND — Environment Variables
# ─────────────────────────────────────────

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=studex_db
DB_USER=postgres
DB_PASS=yourpassword

# JWT
JWT_SECRET=studex_super_secret_key_change_in_production_2024
JWT_EXPIRES_IN=7d

# Server
PORT=5000
NODE_ENV=development

# Frontend URL (for CORS)
CLIENT_URL=http://localhost:5173
